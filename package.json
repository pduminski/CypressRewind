{
  "name": "cypressrewind",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pduminski/CypressRewind.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "cypress": "^15.7.0"
  }
}
